{% extends "emails/layout.html.twig" %}

{% block body %}
<row>
    <columns>
        <h1>Récapitulatif de l'atelier</h1>
        <div class='recap'>
            <img src="{{ email.image('@images/book.png') }}" alt="logo livre">
        </div>
        <p>Bonjour <strong>{{ email.to[0].address }}</strong> !</p>
        <p>Le site boennologie vous envoie les informations relatives aux vins dégustés lors de l'atelier </p>
        <div class='booking' id='reminder'>
            {{ workshop.theme.label }}
        </div>

        <p>Vous pouvez retrouver des informations plus détaillé sur la page de détail de l'atelier depuis le site en renseignant 
        le code suivant en fin dans le formulaire adaptée en bas de page.</p>
        <ul>
            {% for wine in workshop.wines %}
                <li>{{ wine.label }} - {{ wine.productYear }}</li>
            {% endfor %}
        </ul>

        <p>
            Nous vous invitons également à remplir un formulaire de feedback disponible en pièce-jointe du mail ou en ligne en suivant le codeQR suivant: <br>
            <img src="{{ absolute_url(asset('qr_code_feedback.png'))}}" style="width: 12rem; padding: 20px;" />
        </p>

    </columns>
  </row>

{% endblock %}